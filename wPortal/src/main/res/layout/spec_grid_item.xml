<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="80dp"
    android:layout_height="40dp"
    android:background="@drawable/gridview_stroke">

    <LinearLayout
        android:id="@+id/spec_layout"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:orientation="vertical">

        <TextView
            android:id="@+id/spec_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="大份"
            android:textColor="@color/textcolor_black"
            android:textSize="@dimen/text_size_tag" />

        <TextView
            android:id="@+id/spec_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="¥125"
            android:textColor="@color/textcolor_black"
            android:textSize="@dimen/text_size_tag" />
    </LinearLayout>

    <ImageView
        android:id="@+id/add_spec"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_centerInParent="true"
        android:layout_marginBottom="6dp"
        android:layout_marginTop="6dp"
        android:src="@drawable/icon_add_specifications"
        android:visibility="gone" />
</RelativeLayout>